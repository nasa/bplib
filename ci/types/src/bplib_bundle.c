#include "bplib_bundle.h"

void bplib_types_testbundle_init(BPLib_MEM_Pool_t* pool, BPLib_Bundle_t* bundle, int id)
{
    if (pool == NULL || bundle == NULL)
    {
        return;
    }
    return;
}